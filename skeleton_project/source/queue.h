#include <stdlib.h>

int width = 3;
int height = 4;
int** queueMatrix = new int*[width];
for(int i = 0; i < width; ++i)
   queueMatrix[i] = new int[height]; //vil vi ha pekere?
void updateQueue(){
  for (int floor, floor < 4, floor++)
}
